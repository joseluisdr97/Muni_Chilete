
@{
    ViewBag.Title = "Index";
}

<h2>Lista de tipos de tramites del tramite @ViewBag.Tramite.Nombre</h2>
<br />
<div class="container">
    <a class="pull-left btn btn-danger" href="/Tramite/Index"><span class="glyphicon glyphicon-circle-arrow-left"></span>Volver</a>
</div>
<br />
<div class="container">

    <label class="control-label">Ingrese Nombre :</label>
    <div class="row">
        <form action="/Tramite/Buscar">
            <div class="col-sm-4">
                <input id="query" class="form-control" type="text" name="query" value="@ViewBag.datos" placeholder="Ingrese nombre" />
            </div>
            <div class="col-sm-2">
                <button id="btnBuscar" class="btn btn-success"><span class="glyphicon glyphicon-search"></span> Buscar</button>
            </div>
        </form>
        <div class="col-sm-6">
            <a class="btn btn-primary pull-right" id="btnCrear" href="/Tipo_Tramite/Crear?id_Tramite=@ViewBag.Tramite.Id_Tramite"><span class="glyphicon glyphicon-plus"></span> Crear</a>
        </div>
    </div>
    <br />

    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Opción</th>
            </tr>
        </thead>
        <tbody id="tbody">
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id_Tipo_Tramite</td>
                    <td>@item.Nombre</td>
                    <td>@item.Descripcion</td>
                    <td><a href="/Tipo_Tramite/Editar?id=@item.Id_Tipo_Tramite&&id_Tramite=@ViewBag.Tramite.Id_Tramite" class="btn btn-info">Editar</a> | <a href="/Tipo_Tramite/Eliminar?id=@item.Id_Tipo_Tramite&&id_Tramite=@ViewBag.Tramite.Id_Tramite" class="btn btn-danger">Eliminar</a> | <a href="/Requisito/Index?id_Tipo_Tramite=@item.Id_Tipo_Tramite" class="btn btn-success">Requisitos</a></td>
                </tr>
            }
        </tbody>
    </table>

</div>

<script>

    //BUSCAR CUANDO DAMOS CLICK
    $("#btnBuscar").click(function (e) {
        e.preventDefault();
        var valor = $("#query").val();
        $.ajax({ url: "/Tipo_Tramite/Buscar?query=" + valor +"&&id_Tramite="+@ViewBag.Tramite.Id_Tramite })
            .done(function (respuesta) {
            $("#tbody").html(respuesta);
        });
    });

    //BUSCAR CUANDO DAMOS ENTER
    $("#query").keypress(function (e) {
        var code = (e.keyCode ? e.keyCode : e.which);
        if (code == 13) {
            var valor = $("#query").val();
            $.ajax({ url: "/Tipo_Tramite/Buscar?query=" + valor +"&&id_Tramite="+@ViewBag.Tramite.Id_Tramite })
            .done(function (respuesta) {
                $("#tbody").html(respuesta);
            });
        }
    });

</script>

